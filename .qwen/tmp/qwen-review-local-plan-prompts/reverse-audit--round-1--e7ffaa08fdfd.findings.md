# Cumulative findings — T40.5 search-generation lifecycle (local review, round 1)

- R1-1 [Critical] internal/focusedindex/search_generation.go:345 (+ internal/sync/reconcile.go:369-373, internal/indexer/indexer.go:493-496) — Orphaned search transition marker (O_EXCL WriteControlFile) left by a crash before startPublication is never recovered (reclaimCommittedPublicationMarkers gates on IsPublishing; CleanupAbandonedLifecycle and failPublication do not match the state); every later PublishWholeGeneration fails with EEXIST forever; torn marker makes Rollback/Recover hard-error. Empirically reproduced by a finder. — [unverified]
- R1-2 [Critical] internal/focusedindex/search_generation_lifecycle.go:348 and :176-180 — Empty collecting-* directory permanently wedges the collector. Trigger A: final deletion step starting with exactly `budget` entries drains all of them, exits with deleted==budget, and `if more || deleted == budget` returns complete=false without os.Remove(directory) (residue class D ≡ budget-1 mod budget). Trigger B: kill -9 between removing the last entry and os.Remove(directory). Next sweep's collecting branch calls readSearchLifecycleReceipt on the empty dir, gets a non-ENOENT error, controller does not advance cursor on error -> permanent wedge; stale generations accumulate to the 64 cap and admitSearchGenerationGrowth blocks new publications. Empirically probed by a finder. — [unverified]
- R1-3 [Critical] internal/focusedindex/publication.go:138-155 — Candidate generation directory is renamed into search-generations/<repo>/<digest> before the transition marker protects it; sweep holds the same shared mutation lock as publication, and in the window the candidate matches no root/marker/pin; with 3 generations the age-skip gate is off, so the sweep can rename the candidate to collecting-* and drain it -> install fails, rollback discards the completed multi-GiB build (fail-closed, no corruption). — [unverified]
- R1-4 [Critical] internal/focusedindex/search_generation_lifecycle.go:399 — Per-repository namespace directories under search-generations/ are never pruned when empty; after 4,096+1 distinct repositories publish, boundedSearchDirectory errors before any namespace is examined, SweepSearchGenerationLifecycle returns an error every turn, the cursor never advances, and no stale generation in any repository is ever collected again. — [unverified]
- R1-5 [Suggestion] PLAN.md:456 — T40.4 ADR row keeps "(in progress)" although the same diff appends its completion-closure row; convention (T40.3, commit 2c0c430) is an in-place marker update. — [unverified]
- R1-6 [Suggestion] internal/indexer/indexer.go:300-303 — files_offered verbose log prints fallbackReads instead of the offered count; accidentally correct only while batch reads are zero. — [unverified]
- R1-7 [Suggestion] internal/focusedindex/search_generation.go:758-760 (+ :547, :799) — Three full-content validations per publication (stage, previous current, candidate reinstall) under the shared mutation lock; up to ~3 generation-equivalents of hashing/parsing at retained scale; pre-diff publication did zero content reads. — [unverified]
- R1-8 [Suggestion] internal/focusedindex/search_generation_test.go:188-197 — Lifecycle test loop breaks on turn one (directoryA renamed to collecting-* before delete step), so the collecting-resumption lane is never exercised by any test. — [unverified]
- R1-9 [Suggestion] internal/search/searcher.go:1073-1075 — OpenWithGenerationPins / reader-pin fence has zero test coverage (all search tests use Open with nil pins). — [unverified]
- R1-10 [Suggestion] internal/sync/reconcile.go:373-376 — New whole-search RecoverSearchPublication branch in reclaimCommittedPublicationMarkers has no test. — [unverified]
- R1-11 [Suggestion] internal/focusedindex/search_generation.go:268-272 — The 512 MiB/shard, 48 GiB/generation, 96 GiB retained-pair, 256-shard, and file-count refusal fences have no tests. — [unverified]
- R1-12 [Suggestion] internal/focusedindex/search_generation.go:908-910 — RecoverSearchPublication prior-selection and no-match branches are untested. — [unverified]
- R1-13 [Nice to have] internal/indexer/indexer.go:183-185 — AdmitDerived field comment contradicts the new measured call that runs after staging-workspace creation. — [unverified]
- R1-14 [Nice to have] internal/focusedindex/search_generation_lifecycle.go:71-72 — result.More set unconditionally when >=2 namespaces exist; misreports completeness and latently risks permanent backlog mode. — [unverified]
