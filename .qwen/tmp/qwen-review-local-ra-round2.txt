9 auditors required this round — one per chunk. Launch one agent per block below, passing its block VERBATIM — copy, do not retype, and NEVER sample this output (no `| head`): the text IS the deliverable, and a launch reconstructed from a sample matches no record. Blocks are numbered `auditor k of 9`, and the output ends with an end-of-round line — followed by the retirement note, when there is one. If either the numbering or the end-of-round line is missing, the output was truncated in transit; rebuild just the missing chunks with --chunk <id>. Write each Agent call's `description` (the task name the user watches) in your output language, translating the separator label — display only; the prompt stays the block VERBATIM.

───── auditor 1 of 9 — chunk 1 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-1--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=0, limit=156)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 2 of 9 — chunk 2 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-2--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=156, limit=305)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 3 of 9 — chunk 3 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-3--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=461, limit=383)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 4 of 9 — chunk 4 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-4--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=844, limit=77)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 5 of 9 — chunk 5 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-5--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=921, limit=399)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 6 of 9 — chunk 6 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-6--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1320, limit=399)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 7 of 9 — chunk 7 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-7--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1719, limit=170)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 8 of 9 — chunk 8 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-8--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1889, limit=380)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── auditor 9 of 9 — chunk 9 ─────

You are review agent `reverse-audit` — Reverse audit agent (round 2).

## Already confirmed — do not re-report these

These are already on the review; a gap that repeats one is not a gap. Your job is what they missed. This list does not replace the brief; read it first.

The list is a file (23 lines). Read ALL of it, right after your brief — page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--round-2--dbfec60e531a.findings.md")
```

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/reverse-audit--chunk-9--round-2--dbfec60e531a.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=2269, limit=314)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── end of round — 9 auditors ─────
