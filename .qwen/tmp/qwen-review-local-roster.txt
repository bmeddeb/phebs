13 agents required. Launch one agent per block below, passing its block VERBATIM — copy, do not retype. The ───── lines are separators, not part of any prompt. This is the same roster `check-coverage` reads out of the plan: a block you skip or reword is a dimension nobody reviewed. Blocks are numbered `agent k of 13` and the output ends with an end-of-roster line — if either is missing, this output was truncated in transit: every prompt is also recorded on disk, so rebuild just the missing blocks with --chunk <id>, or --role <r> (--file <path> for an invariant agent), plus the same --rules this call was given.

The `description` parameter of each Agent call is the task name the user watches while the agent runs — write it in your output language, translating the block's ───── separator label (keep the role/chunk id visible). Display only: the prompt itself stays the block VERBATIM.

───── agent 1 of 13 — chunk 1 ─────

You are review agent `chunk 1 of 9` — the territory agent for lines 1-156 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-1.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=0, limit=156)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 2 of 13 — chunk 2 ─────

You are review agent `chunk 2 of 9` — the territory agent for lines 157-461 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-2.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=156, limit=305)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 3 of 13 — chunk 3 ─────

You are review agent `chunk 3 of 9` — the territory agent for lines 462-844 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-3.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=461, limit=383)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 4 of 13 — chunk 4 ─────

You are review agent `chunk 4 of 9` — the territory agent for lines 845-921 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-4.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=844, limit=77)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 5 of 13 — chunk 5 ─────

You are review agent `chunk 5 of 9` — the territory agent for lines 922-1320 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-5.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=921, limit=399)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 6 of 13 — chunk 6 ─────

You are review agent `chunk 6 of 9` — the territory agent for lines 1321-1719 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-6.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1320, limit=399)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 7 of 13 — chunk 7 ─────

You are review agent `chunk 7 of 9` — the territory agent for lines 1720-1889 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-7.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1719, limit=170)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 8 of 13 — chunk 8 ─────

You are review agent `chunk 8 of 9` — the territory agent for lines 1890-2269 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-8.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1889, limit=380)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 9 of 13 — chunk 9 ─────

You are review agent `chunk 9 of 9` — the territory agent for lines 2270-2583 of the diff.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/chunk-9.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Your territory is exactly this read; page with a larger `offset` if it comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=2269, limit=314)
```

Report findings in the format your brief specifies, and end with the receipt it names. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 10 of 13 — Test coverage matrix (whole-diff) ─────

You are review agent `test-matrix` — Test coverage matrix (whole-diff).

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/test-matrix.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=0, limit=156)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=156, limit=305)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=461, limit=383)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=844, limit=77)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=921, limit=399)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1320, limit=399)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1719, limit=170)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1889, limit=380)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=2269, limit=314)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 11 of 13 — Agent 1b: Removed-behavior audit ─────

You are review agent `1b` — Agent 1b: Removed-behavior audit.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/1b.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=0, limit=156)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=156, limit=305)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=461, limit=383)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=844, limit=77)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=921, limit=399)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1320, limit=399)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1719, limit=170)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1889, limit=380)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=2269, limit=314)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 12 of 13 — Agent 1c: Cross-file tracer ─────

You are review agent `1c` — Agent 1c: Cross-file tracer.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/1c.brief.md")
```

**The code is a file too — the diff. Nothing in this message contains it.** Read your ranges, and page with a larger `offset` if a read comes back `isTruncated`:

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=0, limit=156)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=156, limit=305)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=461, limit=383)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=844, limit=77)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=921, limit=399)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1320, limit=399)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1719, limit=170)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=1889, limit=380)
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-diff.txt", offset=2269, limit=314)
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── agent 13 of 13 — Agent 7: Build & test verification ─────

You are review agent `7` — Agent 7: Build & test verification.

**Your brief is a file. Read it first — it is the whole of your instructions,
and nothing in this message replaces it.**

```
read_file(file_path="/Users/ben/phebs.com/.qwen/tmp/qwen-review-local-plan-prompts/7.brief.md")
```

Report findings in the format your brief specifies. If you found nothing, say so **and say what you examined** — a return that names nothing you read is indistinguishable from never having read anything.

───── end of roster — 13 agents ─────
