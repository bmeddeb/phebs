# Cumulative findings — T40.5 search-generation lifecycle (local review)

- R1-1 [Critical, confirmed] internal/focusedindex/search_generation.go:345 (+ internal/sync/reconcile.go:369-373, internal/indexer/indexer.go:493-496) — Orphaned search transition marker (O_EXCL WriteControlFile) left by a crash before startPublication is never recovered; every later PublishWholeGeneration fails with EEXIST forever. Probe-confirmed.
- R1-2 [Critical, confirmed] internal/focusedindex/search_generation_lifecycle.go:348 and :176-180 — Empty collecting-* directory permanently wedges the collector. Trigger A: final deletion step starting with exactly `budget` entries returns complete=false without os.Remove(directory) (D ≡ budget-1 mod budget). Trigger B: kill -9 between last entry removal and directory removal. Next sweep's receipt read hard-errors; cursor never advances. Probe-confirmed with fix flip.
- R1-3 [Critical, confirmed] internal/focusedindex/publication.go:138-155 — Candidate generation directory renamed into the namespace before the transition marker protects it; sweep shares the mutation lock, age gate off with 3 directories, candidate matches no root/marker/pin -> swept mid-publication -> install fails, rollback discards the build. Probe-confirmed.
- R1-4 [Critical, confirmed] internal/focusedindex/search_generation_lifecycle.go:399 — Namespace directories never pruned; at 4,097 namespaces boundedSearchDirectory errors every turn and all collection halts permanently. Threshold probe-confirmed.
- R1-5 [Suggestion, confirmed] PLAN.md:456 — T40.4 ADR row keeps "(in progress)" after its completion-closure row was appended; convention is in-place marker update (T40.3, 2c0c430).
- R1-6 [Suggestion, confirmed] internal/indexer/indexer.go:300-303 — files_offered verbose log binds fallbackReads instead of the offered count; accidentally correct while batch reads are zero.
- R1-7 [Suggestion, confirmed] internal/focusedindex/search_generation.go:758-760 (+ :547, :799) — Three full-content validations per publication under the shared mutation lock (up to ~3 generation-equivalents of hashing/parsing at retained scale); ValidateWholeReceipt exists as the no-content alternative.
- R1-8 [Suggestion, confirmed] internal/focusedindex/search_generation_test.go:188-197 — Lifecycle test loop breaks on turn one; the collecting-resumption lane is never exercised. Probe-confirmed (LOOP BROKE AT TURN 0).
- R1-9 [Suggestion, confirmed] internal/search/searcher.go:1073-1075 — OpenWithGenerationPins / reader-pin fence has zero test coverage (all search tests use Open with nil pins).
- R1-10 [Suggestion, confirmed LOW confidence] internal/sync/reconcile.go:373-376 — New whole-search RecoverSearchPublication branch in reclaimCommittedPublicationMarkers has no test. Verifier correction: the "only automatic repair / stays broken until manual reindex" harm claim is refuted — reconcileIndexedRevisions' committed-validation fallback (ValidateWholeReceipt fails while IsPublishing -> ClearRepoIndexState + forced rebuild) repairs the state; a regression costs a full child rebuild, not a permanent outage.
- R1-11 [Suggestion, confirmed] internal/focusedindex/search_generation.go:268-272 — The 512 MiB/shard, 48 GiB/generation, 96 GiB retained-pair, 256-shard, and file-count refusal fences have no boundary tests (only the loop bound references MaxSearchGenerationFiles).
- R1-12 [Suggestion, confirmed] internal/focusedindex/search_generation.go:908-910 — RecoverSearchPublication prior-selection and no-match branches are untested (only the candidate arm runs in the one existing test).
- R1-13 [Nice to have, confirmed] internal/indexer/indexer.go:183-185 — AdmitDerived field comment contradicts the new measured call that runs after staging-workspace creation.
- R1-14 [Nice to have, confirmed LOW confidence] internal/focusedindex/search_generation_lifecycle.go:71-72 — result.More set unconditionally when >=2 namespaces exist. Verifier correction: the "sibling owners set More only on real backlog" premise is contradicted — observationpublication/lifecycle.go:70 and relationshippublication/lifecycle.go:88 contain the identical `result.More = len(repositories) > 1`; the new owner copies an established sibling convention. What the diff changed: this owner's status went from static Exact to permanent lower_bound with >=2 repos. Probe-confirmed mechanism; fixing one owner alone would diverge from its siblings — human call.
- R1-15 [Critical] internal/focusedindex/publication.go:194-195 — Search-generation lifecycle root survives a whole-search -> focused posture flip forever: PublishFocused -> removeRepositoryArtifacts(preserveMarker=true) removes the flat whole artifacts but retains the root; nothing else ever removes it; the sweep protects root.Current/root.Prior unconditionally, permanently pinning up to 96 GiB of generation bytes against collection while the repo stays focused. — [unverified]
- R1-16 [Critical] internal/indexer/indexer.go:460-467 — Ambiguous-commit re-read failure (commit succeeded but GetRepo also fails) resolves as "commit definitely failed": rollback removes both markers and the restored prior root skips ClearRepoIndexState, leaving the committed row diverged from disk; the sync no-op short-circuit blocks rebuilds and the new searcher pin fence returns errWholeGenerationChanged before the whole-repair path can enqueue a forced rebuild -> every query including the repo fails until a new commit or manual force. — [unverified]
- R1-17 [Suggestion] internal/sync/reconcile.go:378-384 — On RecoverSearchPublication error the branch `continue`s, bypassing the pre-existing committed-validation path that can legally retire the marker (legacy repo, crash after startPublication, revision drift: candidate != durable row, Previous == nil) -> persistent error loop and fail-closed search outage until a successful indexer retry pays a full rebuild. — [unverified]
- R1-18 [Suggestion] internal/focusedindex/search_generation.go:633-636 — validateImmutableSearchGeneration requires exact equality of re-measured allocated bytes (st_blocks) with the receipt — a filesystem-dependent property; migrating the data directory to a filesystem with different allocation semantics makes every immutable generation fail deep validation (publication/rollback/recovery), with no self-heal. — [unverified]
- R1-19 [Suggestion] internal/focusedindex/search_generation.go:899-907 — RecoverSearchPublication's candidate branch hand-rebuilds the prospective root (including Prior derivation) instead of calling prospectiveSearchGenerationRoot, duplicating the root-shape policy across recovery and normal completion. — [unverified]
- R1-20 [Critical] internal/focusedindex/search_generation_lifecycle.go:93-95 (+ :54-56, :327) — The sweep hard-errors on any unrecognized entry (e.g. a .DS_Store dropped by Finder on the project's host platform, backup tooling `._*` files), and the controller never advances the owner cursor on error, so a single benign foreign file permanently wedges all search-generation collection; nothing ever removes foreign files under search-generations/. — [unverified]
- R1-21 [Critical] internal/focusedindex/search_generation_lifecycle.go:399 (+ internal/focusedindex/search_generation.go:468-477, internal/lifecycle/search.go:32-35) — Crash-orphaned .stage-/.legacy- directories are skipped by admitSearchGenerationGrowth but count against boundedSearchDirectory's 72-entry namespace budget; enough hard-crashed publications overflow the budget before the sweep's abandoned-stage classification runs, permanently wedging the owner and failing admission for the repo. — [unverified]
