package mcp

// The generated schemas are checked in so clients can validate envelopes
// without running phebs and so schema drift is reviewable.
//go:generate go run ./cmd/schema-gen -out ../../schemas
