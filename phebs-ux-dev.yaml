# Dedicated UX-track dev instance (T43.x browser rounds). Own port and data
# dir so it never collides with the scale track's default instance. The make
# recipes supply the two retained local bundles; receipts clone no remote HEAD.
server:
  addr: "127.0.0.1:3073"
  data_dir: "~/.phebs-ux-dev"
