# Minimal local development config. See docs/config.example.yaml for every
# supported option, default, validation rule, and connector mode.
connections:
  - name: zoekt
    type: git
    url: https://github.com/sourcegraph/zoekt.git
