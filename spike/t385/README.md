# T38.5 microservice product closure receipt

This deterministic source-free receipt closes Epic 38 by composing the exact
retained T37.5 and T38.1–T38.4 receipts with the production and UI gates for
All code discovery, service overview, relationships, comparison, Workbench,
proof, MCP, failure/restart, responsive layout, and accessibility.

Regenerate the canonical receipt for review with:

```sh
go run ./spike/t385/cmd/author
```

The receipt keeps T34.4's five-service search cohort, T33.5's separate
five-service workflow-state cohort, and T37.5's three-repository/four-service
relationship envelope distinct. The neutral add/modify/migrate/retire stories
use only source-free service and evidence-state names. The receipt contains no
source, path, object ID, citation bytes, repository identity, raw error,
private topology, or product measurement. It establishes mechanics only—no
accuracy, completeness, runtime topology, task or Decision authority,
supported scale/SLO, migration/decommission safety, or release authority. The
surfaces remain experimental through Epic 39 and `GATE2-V2` remains
`NOT_ESTABLISHED`.
