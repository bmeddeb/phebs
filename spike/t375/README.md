# T37.5 exact relationship-reader closure receipt

This retained source-free model closes Epic 37 with a neutral three-repository,
four-service topology spanning exact RPC dependency/caller and source-spelled
Kafka producer/consumer rows. `results.json` contains bounded mechanics and
named executable gates only; it retains no source, path, object ID, citation
content, repository identity, raw error, or private topology.

Regenerate the canonical receipt for review with:

```sh
go run ./spike/t375/cmd/author
```

The production gates own authorization-first aggregation, exact current and
historical generation leases, cursor/incarnation fences, byte-span citations,
two-generation comparison, explicit zero/gap/truncation states, optional proof
coverage, Workbench cursor binding, HTTP/MCP parity, recovery, and lifecycle.
The receipt makes no accuracy, completeness, runtime-topology, supported-scale,
SLO, migration/decommission-safety, or release claim; `GATE2-V2` remains
`NOT_ESTABLISHED`.
