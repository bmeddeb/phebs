# T38.3 service-aware Impact and Workbench closure receipt

This deterministic source-free model records the bounded mechanics of the
service-aware Workbench impact composition. The neutral fixture binds exact
source and target service filters to one Investigation revision, citation-free
one-shot relationship snapshots, a repeated final root-set fence, affected and
unowned/ambiguous rows, comparison evidence, and explicit human Dispositions.
It contains no source, path, object ID, repository identity, raw error, private
topology, or product measurement.

Regenerate the canonical receipt for review with:

```sh
go run ./spike/t383/cmd/author
```

Production API and UI tests own authorization, revision/root fences, source and
target invalidation, bounded relationship rows, exact checklist evidence,
responsive rendering, and fixed-category human Dispositions. The receipt makes
no write-impact, task-completion, accuracy, completeness, runtime-topology,
supported-scale, SLO, migration/decommission-safety, or release claim;
`GATE2-V2` remains `NOT_ESTABLISHED`.
