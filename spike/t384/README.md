# T38.4 MCP microservice parity closure receipt

This deterministic source-free model records the bounded mechanics of the
strict MCP projection over the existing service directory, relationship,
citation, and service-aware Workbench impact readers. It contains no source,
path, object ID, citation bytes, repository identity, raw error, private
topology, or product measurement.

Regenerate the canonical receipt for review with:

```sh
go run ./spike/t384/cmd/author
```

Production API and MCP tests own the exact shared request/page types,
authorization and capability darkness, authority and gap preservation,
pagination, hostile-input refusal, cancellation, encoded response ceilings,
tool-count/schema identity, and read-only annotations. The tools return
evidence, not writes, tasks, Decisions, accuracy/completeness or runtime
topology claims, migration/decommission safety, or release authority;
`GATE2-V2` remains `NOT_ESTABLISHED`.
