# T38.2 cross-service explorer closure receipt

This deterministic source-free model records the bounded mechanics of the
table-first cross-service relationship explorer. The neutral fixture has two
authorized repository roots, exact RPC and Kafka rows, broad repository
fallback, explicit gaps, source-first filters, shared table/diagram row IDs,
and immutable citation actions. It contains no source, path, object ID,
citation content, repository identity, raw error, private topology, or product
measurement.

Regenerate the canonical receipt for review with:

```sh
go run ./spike/t382/cmd/author
```

Production API and UI tests own authorization, query/root/cursor fences,
service/direction/evidence/contract-or-topic filters, exact rows, coverage,
page-only visualization identity, citations, responsive layout, and distinct
gap/empty states. The diagram is a deterministic projection of the current
table page and adds no edge or transitivity. The receipt makes no accuracy,
completeness, runtime-topology, supported-scale, SLO,
migration/decommission-safety, or release claim; `GATE2-V2` remains
`NOT_ESTABLISHED`.
